<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".radarr.movie.detail.view.screen.MovieDetailActivity"
    >
    <item
        android:id="@+id/action_movie_search_download"
        android:icon="@drawable/ic_search_black_24dp"
        android:orderInCategory="100"
        android:title="@string/action_search_download"
        app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/action_delete_movie"
        android:icon="@drawable/ic_delete"
        android:title="@string/action_delete_movie"
        app:showAsAction="ifRoom"

        />

</menu>
